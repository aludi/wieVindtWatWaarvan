from app import app
import spacy

'''
@app.route("/")
def helloTest():
    nlp = spacy.load("nl_core_news_sm")
    string = nlp("ik ben hier")
    stringList = []
    for token in string:
        stringList.append(token.text)
        stringList.append(token.pos_)
    print(stringList)
    
    return " ".join(stringList)
    
@app.route("/index")
def weird():
    return "am on the weird page"
'''
